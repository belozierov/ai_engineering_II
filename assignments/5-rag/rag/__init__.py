"""RAG homework package.

Importing this package is side-effect free: it does NOT load the corpus, build
the index, or start a web server. The entrypoint (app.py) does that under
`__main__`. This is what lets eval/eval.py import the package cheaply.
"""
